package joueur;

import java.awt.geom.Point2D;

public class Police extends Joueur {
    public Police(Point2D.Double position) {
        super(position);
    }

    // public List<Jeu> action_possible(Voleur v) {
    // // prendre tous les voisin ou les mvt accessible au policier
    // List<Noeud> voisins = new ArrayList<>();
    // Noeud voleur_noeud = Noeud.distance(this.position);

    // for (Noeud mon_noeud : voleur_noeud.voisin) {
    // if (!issetVoleur(mon_noeud, v)) {
    // voisins.add(mon_noeud);
    // }
    // }

    // // transformez les voisin en jeu

    // }
}
